name: Build Web UI

on:
  workflow_dispatch:
  push:
    paths:
      - "web-ui/**"
      - ".github/workflows/build-web-ui.yml"

permissions:
  contents: read

jobs:
  build:
    name: Validate + Package web-ui
    runs-on: windows-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check required files
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $files = @("web-ui/index.html", "web-ui/css/app.css", "web-ui/js/fp.js", "web-ui/js/api.js", "web-ui/js/mock.js", "web-ui/js/app.js", "web-ui/README.md")
          foreach ($f in $files) {
            if (-not (Test-Path $f)) { throw "missing: $f" }
            Write-Host "ok: $f ($((Get-Item $f).Length) bytes)"
          }

      - name: JS syntax check (node --check)
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          node --version
          foreach ($f in @("web-ui/js/fp.js", "web-ui/js/api.js", "web-ui/js/mock.js", "web-ui/js/app.js")) {
            node --check $f
            Write-Host "syntax ok: $f"
          }

      - name: HTML smoke check (single-page anchors + scripts)
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $html = Get-Content web-ui/index.html -Raw
          # 单页版锚点：环境表 + 指纹面板（旧 8 页 page-* 已不存在，见 build-app.yml 注释）
          $anchors = @("sec-env", "sec-fp", "profileRows", "fpProfile", "fpBrowserDir", "fpCookie")
          foreach ($p in $anchors) {
            if ($html -notmatch $p) { throw "missing anchor: $p" }
            Write-Host "anchor ok: $p"
          }
          foreach ($s in @('js/fp.js', 'js/api.js', 'js/mock.js', 'js/app.js')) {
            if ($html -notmatch [regex]::Escape($s)) { throw "missing script: $s" }
          }
          Write-Host "all scripts referenced"

      - name: Package web-ui.zip
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          if (Test-Path web-ui.zip) { Remove-Item web-ui.zip -Force }
          Compress-Archive -Path web-ui/* -DestinationPath web-ui.zip -Force
          Get-Item web-ui.zip | Format-Table Name, Length

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: web-ui
          path: web-ui.zip
          retention-days: 30
          if-no-files-found: error
