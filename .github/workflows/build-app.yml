name: Build SunLauncher App

# SunLauncher 离线启动器构建（唯一构建工作流；旧 build-launcher/build-offline/
# build-sunlauncher 已删除，逻辑合并于此）。
# 触发：手动 / 推送 launcher/**、web-ui/**、本文件。
# 约束：
#   - 只在 windows-latest 上 MSVC 编译 + node --check + 离线契约校验 + 打包；
#   - 不在本地测试、不启动 SunBrowser、不连接 AdsPower 云端（无 token、无上报）；
#   - 产物 offline-app.zip（SunLauncher.exe + web-ui/ + ports.json 模板 + README）。
# 后端接口来源（只读静态分析，样本未执行）：
#   - F:\manus\AD\asar\asar-full\dist\static\lib\main.min.js（live 版 v2.8.8.7，
#     8.7MB webpack Node 包）：本地 Koa 路由 De.get/De.post 共 162 条，
#     其中 whitelist 免鉴权 20 条；openBrowser/openBrowserV3/checkOpen/
#     getOpenStatus/formatOpenRes 等调用链即启动时序；
#     sunBrowserParams/staticConfig/dynamicConfig 三层指纹 + --extended-parameters
#     换表 Base64 注入；白名单 pathInWhitelist 含 /api/getVersion、/api/cacheSize 等。
#   - F:\manus\AD\asar\reports\ + F:\manus\AD\reports\：PE 静态指纹
#     （151MB x64、无 CLR）、Ghidra 24 片缺件说明、asar 头/文件清单。
#   - 云端 /api/v1/* /api/v2/*（需 login-token）与 PE 原生层不在离线范围，一律不实现。
# UI 逻辑（web-ui/）即契约：
#   - 目录即环境：数据目录子目录名 = profile（如 k1h60tsv_hyg6dd = <fbccId>_<invite>）；
#   - 导入：File System Access 直读目录内 md5(fbcc+"_static/_webrtc/_cookies") 三件套，
#     换表 Base64 解码回填 35+ 指纹表单；CLIENT_HOST 一律剥离，BROWSER_ID 按目录名校正；
#   - 导出/保存：写回三件套（md5 比对一致跳过）+ ui_fingerprint.json 明文侧车（35+ 参数全量）；
#   - 启动：POST /api/start -> C++ FpBuildCmdline 注入 --extended-parameters（含
#     StaticConfig/DynamicConfig/CookiesFile 三文件绝对路径 + UserId + fbcc 噪声种子），
#     spawn SunBrowser.exe + --user-data-dir=<profile> --remote-debugging-port=<分配端口>；
#   - 停止：POST /api/stop -> FpKillProfileTree 按 user-data-dir 树杀 + 句柄兜底；
#   - 代理检测：GET /api/proxy/check?addr=host:port，只做本机 TCP connect（3s 超时），
#     不访问任何云端测速接口；
#   - 以缓存为准：UserId/CanvasMark/WebGLMark/AudioFp/ClientRects/ProxyChain/DeviceName/
#     MacAddress/MediaDevices/TTSEngines/Langs/TimeZone/Geoposition/WebRTCAddress/
#     DisableWebRTC 等保护键在注入与 static/dynamic 写回时被缓存值覆盖，只进 ui 侧车。
# asar 兼容路由（main.cpp 另有 B 组实现，行为对齐 main.min.js）：
#   POST /api/openBrowser /api/openBrowserV3 /api/checkOpen /api/checkProfileOpen
#     /api/batcCloseBrowser /api/updateTabs /api/frontBrowser /api/log
#     /api/clearCache /api/deleteCacheById /api/checkProxy(POST)；
#   GET /api/closeAllBrowser /api/stopAllBrowser /api/getOpenStatus
#     /api/getChromeOpened /api/getVersion /api/getBrowserInfo /api/getTabUrls
#     /api/cacheSize。

on:
  workflow_dispatch:
  push:
    paths:
      - "launcher/**"
      - "web-ui/**"
      - "ports.json"
      - ".github/workflows/build-app.yml"

permissions:
  contents: read

jobs:
  build:
    name: MSVC build + offline checks + package
    runs-on: windows-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Build SunLauncher (C++17 Win32, Release x64)
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          msbuild launcher\cpp\SunLauncher.vcxproj /p:Configuration=Release /p:Platform=x64
          $exe = Get-ChildItem launcher\cpp\x64\Release\SunLauncher.exe -ErrorAction Stop
          Write-Host "built: $($exe.FullName) ($($exe.Length) bytes)"
          if ($exe.Length -lt 10240) { throw "SunLauncher.exe suspiciously small" }

      - name: Check web-ui required files
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $files = @("web-ui/index.html", "web-ui/css/app.css", "web-ui/js/fp.js",
                     "web-ui/js/api.js", "web-ui/js/mock.js", "web-ui/js/app.js", "web-ui/README.md")
          foreach ($f in $files) {
            if (-not (Test-Path $f)) { throw "missing: $f" }
            Write-Host "ok: $f ($((Get-Item $f).Length) bytes)"
          }
          if (-not (Test-Path "ports.json")) { throw "missing: ports.json template" }
          Write-Host "ok: ports.json"

      - name: JS syntax check (node --check, no execution)
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          node --version
          foreach ($f in @("web-ui/js/fp.js", "web-ui/js/api.js", "web-ui/js/mock.js", "web-ui/js/app.js")) {
            node --check $f
            Write-Host "syntax ok: $f"
          }

      - name: Offline contract check (no cloud / no token / routes complete)
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          # 1. web-ui 不得引用云端域名 / token / 云端路由 / 旧页面锚点
          $banned = @("adspower.net", "adspower.com", "adspowerapp.com", "loginkits.com", "xmp.one",
                      "acczing.com", "login-token", "apiKey", "api-key", "get-token",
                      "/api/v1/", "/api/v2/", "/api/tabAssistant", "ENV-",
                      "activeProfile", "page-profiles", "page-groups", "xrkClientAPI")
          foreach ($f in @("web-ui/index.html", "web-ui/js/api.js", "web-ui/js/mock.js", "web-ui/js/app.js", "web-ui/js/fp.js")) {
            $t = Get-Content $f -Raw
            foreach ($b in $banned) {
              if ($t -match [regex]::Escape($b)) { throw "banned string '$b' in $f" }
            }
            Write-Host "clean: $f"
          }
          # 2. C++ 指纹模块必备符号（换表/三件套/注入/树杀/侧车）
          $cpp = Get-Content launcher\cpp\fingerprint.cpp -Raw
          foreach ($s in @("FpEncode", "FpDecode", "FpMd5Hex", "FpBuildCmdline", "FpKillProfileTree",
                           "FpStaticName", "FpDynamicName", "FpCookiesName",
                           "hTy1bfRJz4nLPcBCO7WtmNIaGvVeul5Zo8kq32UxrYw_-0gsjp96SDFXQiEMKdHA",
                           "ui_fingerprint.json")) {
            if ($cpp -notmatch [regex]::Escape($s)) { throw "missing symbol '$s' in fingerprint.cpp" }
            Write-Host "symbol ok: $s"
          }
          # 3. C++ 离线 HTTP 路由齐备：
          #    A 组离线自有（web-ui/api.js 契约）：profiles/start/stop/fp/proxy/check + serveFile；
          #    B 组 asar 兼容（main.min.js 本地 Koa 路由离线版，见 main.cpp 注释）：
          #    openBrowser/openBrowserV3/checkOpen/checkProfileOpen/batcCloseBrowser/
          #    closeAllBrowser/stopAllBrowser/getOpenStatus/getChromeOpened/getVersion/
          #    getBrowserInfo/getTabUrls/cacheSize/clearCache/deleteCacheById/
          #    checkProxy(POST)/updateTabs/frontBrowser/log。
          $main = Get-Content launcher\cpp\main.cpp -Raw
          foreach ($r in @("/api/profiles", "/api/start", "/api/stop", "/api/fp/", "/api/fp/save",
                           "/api/proxy/check", "serveFile",
                           "/api/openBrowser", "/api/openBrowserV3", "/api/checkOpen",
                           "/api/checkProfileOpen", "/api/batcCloseBrowser",
                           "/api/closeAllBrowser", "/api/stopAllBrowser",
                           "/api/getOpenStatus", "/api/getChromeOpened", "/api/getVersion",
                           "/api/getBrowserInfo", "/api/getTabUrls", "/api/cacheSize",
                           "/api/clearCache", "/api/deleteCacheById", "/api/checkProxy",
                           "/api/updateTabs", "/api/frontBrowser", "/api/log")) {
            if ($main -notmatch [regex]::Escape($r)) { throw "missing route '$r' in main.cpp" }
            Write-Host "route ok: $r"
          }
          # 4. 前端 API 封装与路由一一对应（含 checkProxy）
          $api = Get-Content web-ui\js\api.js -Raw
          foreach ($r in @("/api/profiles", "/api/start", "/api/stop", "/api/fp/", "/api/fp/save", "/api/proxy/check")) {
            if ($api -notmatch [regex]::Escape($r)) { throw "missing api '$r' in web-ui/js/api.js" }
            Write-Host "api ok: $r"
          }
          # 5. 脚本引用顺序（fp.js 编解码必须在 api.js 之前）
          $html = Get-Content web-ui/index.html -Raw
          $iFp = $html.IndexOf("js/fp.js"); $iApi = $html.IndexOf("js/api.js")
          if ($iFp -lt 0 -or $iApi -lt 0 -or $iFp -gt $iApi) { throw "fp.js must load before api.js" }
          Write-Host "script order ok"
          # 6. mock 覆盖全部离线路由（file:// 演示模式不断链）
          $mock = Get-Content web-ui\js\mock.js -Raw
          foreach ($r in @("/api/profiles", "/api/start", "/api/stop", "/api/fp/", "/api/fp/save")) {
            if ($mock -notmatch [regex]::Escape($r)) { throw "missing mock '$r' in web-ui/js/mock.js" }
            Write-Host "mock ok: $r"
          }

      - name: Package offline-app.zip
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $out = "offline-pkg"
          if (Test-Path $out) { Remove-Item $out -Recurse -Force }
          New-Item -ItemType Directory -Path $out | Out-Null
          Copy-Item launcher\cpp\x64\Release\SunLauncher.exe (Join-Path $out "SunLauncher.exe") -Force
          Copy-Item web-ui (Join-Path $out "web-ui") -Recurse -Force
          Copy-Item ports.json (Join-Path $out "ports.json") -Force
          @"
          # SunLauncher 离线启动器包（本机使用，不联网、不上传、不与 AdsPower 云端通信）
          # 1. 把 SunLauncher.exe 放到任意目录，双击运行（Win32 原生窗口：profile 列表 + 启动/关闭）
          # 2. 数据目录默认 F:\.ADSPOWER_GLOBAL\cache（窗口里可改；子目录名即环境，如 k1h60tsv_hyg6dd）
          # 3. 浏览器目录默认 ...\cwd_global\chrome_152（窗口里可改；cmd.Dir 必须设在含版本子目录那一级）
          # 4. UI：窗口启动后访问 http://127.0.0.1:18900/（SunLauncher 内置 serving web-ui/）；
          #    也可 file:// 双击 web-ui/index.html（演示模式：目录导入可用，启动/保存为 mock）
          # 5. UI 逻辑：导入=解码三件套回填 35+ 参数（CLIENT_HOST 剥离）；保存=写回三件套 + ui_fingerprint.json；
          #    启动=--extended-parameters 指纹注入；停止=按 user-data-dir 树杀；代理检测=本机 TCP connect
          # 6. 以缓存为准：保护键（UserId/CanvasMark/WebGLMark/ProxyChain/TimeZone 等）在注入与写回时被缓存值覆盖
          # 7. 调试：点启动后无窗口，看 exe 同目录 debug.log（完整命令行 / 3 秒退出码 / [browser] 输出）
          "@ | Out-File (Join-Path $out "README.txt") -Encoding utf8
          if (Test-Path offline-app.zip) { Remove-Item offline-app.zip -Force }
          Compress-Archive -Path "$out\*" -DestinationPath offline-app.zip -Force
          Get-Item offline-app.zip | Format-Table Name, Length

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: offline-app
          path: offline-app.zip
          retention-days: 30
          if-no-files-found: error
